Account login data (user id, username, password) are kept in _config.php_

Create a new file called _config.php_ with this syntax:

`define("USER_ID", "value");`

`define("USERNAME", "value");`

`define("PASSWORD", "value");`
